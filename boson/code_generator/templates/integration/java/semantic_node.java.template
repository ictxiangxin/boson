{% if configure.boson_option['code_comment'] == 'yes' -%}
/*
    {{ configure.boson_title }} - {{ configure.boson_description }}

        Author: {{ configure.boson_author }}
        Email:  {{ configure.boson_email }}
        Site:   {{ configure.boson_url }}

    File: "{{ configure.boson_option['semantic_node_class_name'] }}.java"
    These codes ware generated by the boson Java code generator.
*/

{% endif -%}
package boson;

import java.util.ArrayList;
import java.util.List;

public class {{ configure.boson_option['semantic_node_class_name'] }}<T> {
    public {{ configure.boson_option['semantic_node_class_name'] }}() {
    }

    public {{ configure.boson_option['semantic_node_class_name'] }}(T data) {
        this._data = data;
    }

    public void makeNull() {
        this._isNull = true;
    }

    public boolean isNull() {
        return this._isNull;
    }

    public T getData() {
        return this._data;
    }

    public void setData(T data) {
        this._data = data;
    }

    public int getReduceNumber() {
        return this._reduceNumber;
    }

    public void setReduceNumber(int reduceNumber) {
        this._reduceNumber = reduceNumber;
    }

    public String getText() {
        return this._text;
    }

    public void setText(String text) {
        this._text = text;
    }

    public void append({{ configure.boson_option['semantic_node_class_name'] }}<T> item) {
        this._children.add(item);
    }

    public void insert(int index, {{ configure.boson_option['semantic_node_class_name'] }}<T> item) {
        this._children.add(index, item);
    }

    public List<{{ configure.boson_option['semantic_node_class_name'] }}<T>> children() {
        return this._children;
    }

    public {{ configure.boson_option['semantic_node_class_name'] }}<T> get(int index) {
        return this._children.get(index);
    }

    static public {{ configure.boson_option['semantic_node_class_name'] }}<T> nullNode() {
        {{ configure.boson_option['semantic_node_class_name'] }}<T> node = {{ configure.boson_option['semantic_node_class_name'] }}<T>();
        node.makeNull();
        return node;
    }

    protected int _reduceNumber = -1;
    protected String _text;
    protected List<{{ configure.boson_option['semantic_node_class_name'] }}<T>> _children = new ArrayList<>();
    protected T _data;
    protected boolean _isNull;
}

